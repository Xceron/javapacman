plugins {
    id "java"
    id "application"
    id "org.sonarqube" version "3.5.0.2730"
}

//mainClassName = "Pacman"

sonarqube {
    properties {
        property "sonar.projectKey", "Xceron_javapacman"
        property "sonar.organization", "xceron"
        property "sonar.host.url", "https://sonarcloud.io"
    }
}